//#include "EventLoop.h"
#include <stdio.h>
//#include "thread/Thread.h"
#include<iostream>

/*
void threadFunc()
{
    printf("threadFunc() : pid = %d, tid = %d \n",
            getpid(), muduo :: CurrentThread::tid());
    SUNSQ::EventLoop loop;
    loop.loop();
}
*/

int main(){
    /*printf("threadFunc() : pid = %d, tid = %d \n",
            getpid(), 555);

    SUNSQ::EventLoop loop;

    //muduo::Thread thread(threadFunc);
    //thread.start();

    loop.loop();
    pthread_exit(NULL);
*/
    std::cout<< " puzzled, why wrong again??";

    return 0;
}
